# 分析に使う Python パッケージ。**入れたらここに書く。**
#
#   octavo env                     # uv で .venv を作り、このファイルのものを入れる
#                                  # （ここを書き換えたら、また走らせる）
#
# 入れ足したら `uv pip freeze > requirements.txt` ではなく、使うものだけを
# 手で足して版を固定する（freeze は依存まで全部書き出して読めなくなる）。
# R 側は renv が同じ役目をする（renv.lock）。

# pandas==2.3.1
# statsmodels==0.14.5
